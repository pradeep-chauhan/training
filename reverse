#include < stdio.h>
int main()
{
    int n , rev;    
    n = 1234;
    rev = 0; 
    while (n != 0 ){
        rev = (rev*10  + n%10);
        n = n /10;
        
    }
   printf("rev is %d" , rev);
    return 0;
}

Output ⇒ 4321

